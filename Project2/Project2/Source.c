#include <stdio.h>

int main() {
  
        printf("Число %d - чётное.\n", number);
        int main() {
            int number;

            

            if (number % 2 == 0) {
                printf("Число %d - чётное.\n", number);
            }
            else {
                printf("Число %d - нечётное.\n", number);
            }

            return 0;
        }
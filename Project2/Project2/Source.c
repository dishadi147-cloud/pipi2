#include <stdio.h>

int main() {
  
        printf("„исло %d - чЄтное.\n", number);
        int main() {
            int number;

            printf("¬ведите целое число: ");
            scanf("%d", &number); // —читываем число с клавиатуры

            printf("¬ведите целое число: ");
            scanf("%d", &number); // —читываем число с клавиатуры
            if (number % 2 == 0) {
                printf("„исло %d - чЄтное.\n", number);
            }
            else {
                printf("„исло %d - нечЄтное.\n", number);
            }

            return 0;
        }